spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  # Optional: Configure logging for better test debugging
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
